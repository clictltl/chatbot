import { createApp } from 'vue';
import App from './App.vue';
// test gp
// import { checkLogin } from './auth';

createApp(App).mount('#app');

// test gp
// checkLogin().then(() => {
//   createApp(App).mount('#app');
// });