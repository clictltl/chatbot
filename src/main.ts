import { createApp } from 'vue';
import App from './App.vue';
// import { checkLogin } from './auth';

createApp(App).mount('#app');

// checkLogin().then(() => {
//   createApp(App).mount('#app');
// });